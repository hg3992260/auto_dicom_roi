# DICOM 自动化分析工具 交接文档

## 1. 项目概述

基于 PyCt6 + PySide6 的 DICOM 医学图像分析桌面工具，集成 SAM (Segment Anything) AI 分割和传统 CV ROI 检测引擎。

**启动入口**: `main.py`  
**Conda 环境**: `RSNA311`  
**Python 路径**: `D:\python\envs\RSNA311\python.exe`

## 2. 目录结构

```
F:\RSNA\dicom_analysis_tool/
├── main.py                   # 入口 (CApplication → CMainWindow)
├── dicom_cluster.py          # DICOM 扫描 + 按 Patient/Study/Series 自动分组
├── roi_engine.py             # ROI 检测引擎 (5 种算法 + overlay)
├── sam_engine.py             # SAM 模型单例 (ViT-B/ViT-L/ViT-H 加载/推理)
├── utils.py                  # 工具函数 (PNG 叠加渲染、JSON 保存)
├── requirements.txt
├── 交接文档.md
│
├── models/                   # SAM 模型文件 (~4.0 GB)
│   ├── sam_vit_b_01ec64.pth  # ViT-B  358 MB  STANDARD
│   ├── sam_vit_l_0b3195.pth  # ViT-L  1.2 GB  STANDARD+
│   └── sam_vit_h_4b8939.pth  # ViT-H  2.4 GB  HI-PRECISION
│
├── ui/
│   ├── __init__.py
│   ├── main_window.py        # 主窗口 (三栏布局 + UnifiedViewer)
│   ├── dicom_tree.py         # QTreeWidget (Patient→Study→Series→File)
│   ├── roi_panel.py          # ROI 检测控制面板
│   └── sam_panel.py          # SAM 分割控制面板
│
└── output/                   # 结果输出目录
    ├── roi_overlay/          # ROI overlay 检测 JSON
    ├── roi_otsu/             # Otsu 批量 JSON
    └── *_sam_roi.*           # SAM 交互式 ROI JSON + PNG
```

## 3. 核心模块

### 3.1 dicom_cluster.py — DICOM 扫描与分组

```python
from dicom_cluster import DicomCluster
cluster = DicomCluster()
patients = cluster.scan_folder(folder_path)  # → Dict[str, PatientInfo]
summary  = cluster.get_summary(patients)     # → {patient_count, file_count, ...}
```

- 递归扫描文件夹，自动识别 DICOM 文件 (检查 `DICM` header)
- 按 `PatientID` → `StudyInstanceUID` → `SeriesInstanceUID` 三级分组
- 支持进度回调 `progress_callback(current, total)`

### 3.2 roi_engine.py — ROI 检测引擎

```python
from roi_engine import RoiEngine
engine = RoiEngine()
rois = engine.detect(file_path, method="overlay", params={"min_area": 50})
```

| 方法 | 算法 | 说明 |
|------|------|------|
| `overlay` | DICOM Overlay Plane (6000 组) | 3 级 fallback: pydicom 内置 → 手动位解包 → RT Structure Set |
| `otsu` | OTSU 阈值 + 形态学 (闭+开) | 椭圆结构元 |
| `adaptive` | 自适应高斯阈值 + 形态学 (闭) | 可调 block_size, c_value |
| `watershed` | 分水岭算法 | 距离变换 + 标记 |
| `edge` | Canny 边缘检测 + 形态学 (闭) | |

轮廓过滤: **面积** → **长宽比** (≤ 5.0) → **圆形度** (≥ 0.1)

```python
# 直接获取 overlay 原始位图掩膜
mask = engine.extract_overlay_mask(file_path)  # → np.ndarray (bool)
```

### 3.3 sam_engine.py — SAM 分割引擎

```python
from sam_engine import SamEngine
engine = SamEngine()                        # 单例，自动检测 CUDA
engine.set_checkpoint(path)                 # 切换模型
engine.load_model(callback)                 # 加载模型
engine.set_image(rgb_image)                 # 设置输入图像
masks, scores, logits = engine.predict_mask(points, labels)  # 点提示推理
rois = engine.auto_segment(image)           # 全自动分割
```

- 支持 ViT-B / ViT-L / ViT-H 三种量级模型
- 自动 GPU/CPU fallback
- 单例模式，切换 checkpoint 自动卸载旧模型

### 3.4 ui/main_window.py — UnifiedViewer

核心图像显示组件，统一处理三种模式：

| 模式 | 触发 | 显示内容 |
|------|------|---------|
| Viewing | 默认 | 灰度图 + 窗宽窗位 |
| Overlay | ROI→点击"开始ROI检测"→overlay | 灰度图 + 绿色掩膜叠加 |
| SAM | 切换到"SAM分割"标签 | RGB 图 + 绿色掩膜 + 前景/背景点 |

```python
viewer.load_dicom(path)    # 加载 DICOM → 显示
viewer.set_window(wc, ww)  # 窗宽窗位实时调整
viewer.set_mask(mask)      # 叠加掩膜 (ROI 或 SAM)
viewer.set_sam_mode(True)  # 切换 SAM 交互模式
```

## 4. 安装部署

```bash
cd F:\RSNA\dicom_analysis_tool
pip install PyCt6 PySide6 pydicom numpy opencv-python scikit-image Pillow
pip install torch segment-anything
```

**Conda 环境启动**:
```bash
conda activate RSNA311
python main.py
```
或直接用解释器:
```bash
D:\python\envs\RSNA311\python.exe main.py
```

## 5. 使用指南

### 5.1 扫描 DICOM

1. 拖入文件夹 或 浏览选择 DICOM 根目录
2. 点击「扫描DICOM文件」
3. 左侧摘要栏显示统计，病人列表树自动展开
4. 右侧自动显示第一个病人的第一张图像

### 5.2 图像浏览

- **窗宽窗位**: 右侧滑块实时调整对比度/亮度
- **↺ 按钮**: 重置窗宽窗位到 DICOM 默认值
- **点击树节点**: 切换显示对应 DICOM 图像

### 5.3 ROI 检测 (默认 overlay)

1. 切换到「ROI检测」标签
2. 方法选 `overlay` (默认)
3. 点击「开始ROI检测」
4. 右侧图像立即叠加绿色 ROI 掩膜
5. 后台自动批处理所有选中文件 → 保存 JSON

### 5.4 SAM 分割

1. 切换到「SAM分割」标签
2. 点击模型路径按钮 → 选择 `.pth` 文件
3. 点击「确认加载」→ 加载完成 (高对比色状态提示)
4. 在右侧图像上：
   - **左键** = 前景标记 (绿色点)
   - **右键** = 背景标记 (红色点)
5. 实时显示 SAM 分割 mask
6. 点击「保存ROI」→ 确认 DICOM 元数据 + ROI 统计 → JSON + PNG

## 6. 配色方案

| 元素 | 色值 | 说明 |
|------|------|------|
| 工具栏背景 | `#1E3A5F` | 深钴蓝 |
| 工具栏文字 | `#FFFFFF` | 白色 |
| 标题 | `#0B1E35` | 深海军蓝, Bold |
| 侧边栏 | `#F0F4F8` | 浅灰蓝 |
| 卡片背景 | `#FFFFFF` | 白色 |
| 卡片边框 | `#93B4E8` | 柔和蓝, 2px |
| 主按钮 | `#2563EB` | 亮蓝 |
| 扫描按钮 | `#059669` | 翠绿 |
| SAM Hi-Precision | `#7C3AED` | 紫色 (特殊标识) |
| SAM Standard+ | `#D97706` | 橙色 (特殊标识) |

## 7. 输出格式

### ROI 检测 JSON
```json
{
  "method": "overlay",
  "params": {"min_area": 50},
  "file_count": 6,
  "results": [{
    "file": "path/to/file",
    "rois": [{
      "roi_id": "roi_0",
      "bbox": {"x": 409, "y": 264, "width": 7, "height": 12},
      "area": 36.0,
      "circularity": 0.239,
      "contour": [[...], ...],
      "centroid": {"x": 412, "y": 270},
      "source": "roi_overlay"
    }]
  }]
}
```

### SAM ROI JSON
```json
{
  "file": "path/to/file",
  "timestamp": "20260728_140000",
  "source": "magic_seg_interactive",
  "metadata": {
    "患者姓名": "CHENQIAN",
    "检查日期": "20250610",
    ...
  },
  "roi_statistics": {
    "像素数": 1234,
    "面积(mm²)": "45.67",
    "均值": "128.34 HU",
    "标准差": "23.45 HU",
    ...
  }
}
```

## 8. 已知注意事项

1. **SAM 模型加载**: 首次加载约 10-30 秒 (取决于模型大小和硬件)
2. **内存占用**: ViT-H 加载后约需 8+ GB GPU 显存
3. **Overlay 识别**: 依赖 DICOM 文件包含 6000 组 Overlay Plane (Siemens Syngo.via 后处理图像)
4. **无数据库依赖**: 所有结果直接输出 JSON/PNG 文件
5. **拖放支持**: 主窗口支持拖入文件夹自动开始扫描
